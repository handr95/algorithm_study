package array;

/**
 * 배열과 문자열
 *
 * 1.7 행렬 회전
 * 이미지를 표현하는 NxN 행렬이 있다.
 * 이미지의 각 픽셀은 4바이트로 표현된다.
 * 이때, 이미지를 90도 회전시키는 메서드를 작성하라.
 * 행렬을 추가로 사용하지 않고서도 할 수 있겠는가?
 */
public class MatrixRotation {
    public String matrixRotation(String a){
        return "";
    }
}
