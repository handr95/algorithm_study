package array;

/**
 * 배열과 문자열
 * 1.2 순열 확인
 * 문자열 두 개가 주어졌을 때 이 둘이 서로 순열 관계에 있는지 확인하는 메소드를 작성하라.
 *
 * 순열 : 서로 다른 nn개의 원소에서 rr개를 중복없이 골라 순서에 상관있게 나열하는 것을 nn개에서 rr개를 택하는 순열이라고 한다
 */
public class PermutationCheck {

    public boolean permutation_check(String a, String b) {

        return false;
    }
}
